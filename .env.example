# AWS credentials: uses ~/.aws/credentials (aws cli configure)
# No need to set AWS keys here if AWS CLI is configured

# SES email config
SES_SENDER_EMAIL=jpagolu@mitresource.com
SES_RECIPIENT_EMAIL=akommu@mitresource.com
